# CloudNote 第三优先级修改说明

## 本次任务

本次修改为第三优先级“后台任务列表页二次优化”。目标是在第一次任务列表视觉升级基础上继续补修，不重做系统，不改成前后端分离，不引入 Vue / React / Next.js，不修改 CloudNote / 云笺品牌名称。

## 修改文件

| 文件 | 主要修改 |
|---|---|
| `public/admin.html` | 移除任务列表页状态筛选中的“草稿”选项；移除没有高级筛选功能的“筛选”按钮；保留现有任务列表整体结构、侧边栏、统计区、任务卡片和分页区域。 |
| `public/main.js` | 移除任务列表页“草稿”统计和筛选逻辑；为任务搜索增加 300ms 防抖；统计卡片改为 4 项真实数据；提交记录和统计报告任务下拉改为分页拉取全部未删除任务；存储空间卡片不再写死进度比例。 |
| `public/style.css` | 将统计卡片调整为 4 张等宽铺满；统一搜索框、状态筛选和排序下拉的高度、圆角、边框和阴影；优化任务卡片说明、信息标签、操作按钮、分页禁用态和存储卡片无容量上限状态。 |

## UI 优化

1. 任务列表页统计卡片仅保留“全部任务、进行中、已完成、已删除”。
2. 顶部筛选区移除无实际高级筛选功能的按钮，保留搜索、状态筛选、排序。
3. 四张统计卡片等宽排列，图标、标题、数字和说明文字保持统一对齐。
4. 任务卡片内“暂无说明”视觉弱化，信息标签高度和间距统一。
5. 操作按钮继续保留复制链接、查看提交记录、编辑、下载任务、删除，并保持右侧分组排布。
6. 分页按钮增加更明显的禁用态，当前页保持绿色高亮。
7. 存储空间在没有总容量配置时不再显示误导性进度百分比。

## 修复的逻辑问题

1. 移除任务列表页“草稿”假功能，只保留创建任务页已有的本地保存草稿能力。
2. 搜索框改为 300ms 防抖，避免每输入一个字符就立即请求接口。
3. 状态筛选、排序和每页数量切换仍会立即刷新，并将当前页重置为第 1 页。
4. 统计报告任务筛选下拉和提交记录任务筛选下拉不再只使用任务列表当前页数据，而是加载全部未删除任务。
5. 任务列表分页仍只加载当前页，不影响列表性能和原有分页逻辑。
6. 存储空间卡片不再写死 `14%` 这类假进度。

## 需要人工浏览器确认

1. 后台任务列表页四张统计卡片在桌面和窄屏下排列是否符合预期。
2. 搜索任务名称、任务链接或任务 ID 时，停止输入约 300ms 后列表是否刷新。
3. 点击统计卡片、状态筛选、排序和每页显示后，列表是否回到第 1 页并刷新正确。
4. 点击任务卡片右侧“查看提交记录”后，是否切换到提交记录区域并带入当前任务筛选。
5. 统计报告和提交记录页面的任务下拉是否包含全部未删除任务。
6. 左侧存储空间卡片在无总容量配置时是否只显示“已用容量：xxx（总容量未配置）”，且不显示误导性进度。
