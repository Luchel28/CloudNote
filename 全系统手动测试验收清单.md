# CloudNote 新平台手动测试验收清单

> 本清单只面向当前正式平台。

## 正式入口

- [ ] 打开 `/admin/`，刷新后仍能正常加载
- [ ] 打开 `/student/`，刷新后仍能正常加载
- [ ] 分享链接优先使用 `/student/?code=分享码`
- [ ] 通过任务 ID 访问时使用 `/student/?id=任务ID`
- [ ] 后端接口仅使用 `/api/v1/*`
- [ ] 学生侧公开接口仅使用 `/api/v1/open/*`

## 自动化验收

- [ ] `npm run typecheck` 通过
- [ ] `npm run build:platform` 通过
- [ ] `npm run smoke:platform` 通过
- [ ] `npm run smoke:platform:web` 通过
- [ ] `npm run smoke:platform:web-apps` 通过
- [ ] `npm run smoke:platform:backup` 通过
- [ ] `npm run smoke:platform:verify` 通过

## 人工核心流程

- [ ] 管理员可以登录管理端
- [ ] 可以创建、编辑、发布、删除和恢复任务
- [ ] 学生可以通过分享码打开任务并提交文件
- [ ] 后台提交记录、统计报告、回收站和存储统计数据正确
- [ ] 单文件下载、整任务打包下载和 CSV 导出正常
- [ ] 截止、关闭任务、重复提交限制和必填字段校验符合预期

## 最终结论

```text
结论：
风险：
补充说明：
```
