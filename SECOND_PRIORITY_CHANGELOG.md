# CloudNote 第二优先级修改说明

更新时间：2026-04-29

## 本次范围

本次只处理第二优先级：创建/编辑任务页面、字段编辑、任务模板、回收站、学生上传页和后台提交展示联动。未进行阿里云部署，未接入 OSS，未调整第一优先级已完成的上线基础与安全配置。

## 创建/编辑任务页面

- 保留现有三步式创建任务入口，并强化底部固定操作栏，滚动时仍可操作“保存草稿 / 保存模板 / 创建或保存 / 取消编辑”。
- 编辑任务时页面标题和主按钮会切换为“编辑任务 / 保存修改”，避免管理员误判当前状态。
- 文件限制、重复提交、逾期提交、打包方式、上传必填等配置继续沿用原字段，不新增冲突字段。

## 字段编辑与自定义字段

- 字段名称输入框可直接连续输入、删除和修改。
- 字段临时为空不会删除字段，删除只能通过明确的删除按钮触发，并带确认提示。
- 字段名称为空时显示“字段名称不能为空”，不会自动保存非法字段名。
- 支持 Enter、失焦、确认按钮提交字段名称修改。
- 字段类型、必填、最大长度、数字规则、占位提示、字段说明、单选/多选选项会稳定保存，字段排序不会因编辑名称而混乱。
- 学生上传页已支持单选、多选字段渲染和必填校验。

## 模板功能

- 新增 SQLite 表 `assignment_templates`，沿用项目现有数据库路径配置 `CLOUDNOTE_DB_PATH`。
- 新增模板 API：
  - `GET /api/templates`
  - `POST /api/templates`
  - `PUT /api/templates/:id`
  - `DELETE /api/templates/:id`
- 创建/编辑任务页可保存模板；模板保存任务说明、自定义字段、文件限制、截止时间默认值等配置，不保存学生提交数据。
- 模板管理支持查看、套用、预览、编辑、删除和批量删除。
- 旧版浏览器本地模板仍保留兼容，并可继续在本地回收站恢复。

## 回收站功能

- 删除任务继续采用软删除，默认任务列表不显示已删除任务。
- 已删除任务的学生链接会返回“该任务已关闭或已删除，不能继续提交”。
- 回收站支持任务、文件、模板的查看、恢复、彻底删除和清空。
- 恢复任务时只恢复本次任务删除一起隐藏的提交和文件，避免把之前单独删除的文件误恢复。
- 永久删除任务会清理关联提交记录和实际上传文件，避免孤立数据或残留文件。

## 学生端与后台联动

- 学生端展示任务名称、任务说明、截止时间、字段说明、文件规则。
- 必填字段、字段格式、文件数量、文件大小、文件扩展名、截止时间限制均进行前后端校验。
- 后台提交列表会按字段中文名展示学生填写结果。
- 下载单文件、打包下载、上传成功展示保持兼容。

## 验证记录

- 已执行 `node --check server.js`。
- 已执行 `node --check public/main.js`。
- 已启动本地服务并验证：
  - 创建任务；
  - 保存模板；
  - 获取学生上传页配置；
  - 必填字段缺失返回 400；
  - 合法无文件提交返回 201；
  - 删除任务后学生链接返回 410；
  - 回收站恢复任务；
  - 回收站彻底删除任务和模板。
